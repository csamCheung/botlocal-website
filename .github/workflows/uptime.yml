name: uptime

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: admin app (app.botsquirrel.com)
        run: curl -sf -o /dev/null --max-time 30 --retry 2 https://app.botsquirrel.com/login
      - name: engine (rag-chatbot-production)
        run: curl -sf -o /dev/null --max-time 30 --retry 2 https://rag-chatbot-production-5fcc.up.railway.app/
